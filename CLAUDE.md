# FORM / Setline Codex

Read SPEC.md and PLAN.md. The current entry is index.html → form/app.js. Reuse the tested plain JS engine and preserve IndexedDB/kg history. Keep private training profiles outside Git. Run node --test and optional tools/form-browser-check.cjs before release. Publish only to Setline-Codex, never original Setline. Previous specifications live under docs/.
